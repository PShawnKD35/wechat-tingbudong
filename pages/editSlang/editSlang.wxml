<cu-custom bgColor="bg-gradual-blue" isCustom="{{true}}"><view slot="content">听不懂</view></cu-custom>

 <!--copying from pages/addslang-->
 <view class="blue">
  <view class="title">{{name}}</view>
     <view class="white">
      
      <view class="form-group">
     
        <!--category input-->
        <view class="section__title">Add new tags</view>
          <view class="input">
            <input type="text" bindinput="slangTag" name="slangTag" maxlength="60" placeholder="#chinese #东北话 #流行语" />
          </view>

        <!--uploadImage input-->
<view class="section__title">上传一个表情 </view>
	<view class="cu-form-group">
		<view class="grid col-4 grid-square flex-sub">
			<view class="bg-img" wx:for="{{imgList}}" wx:key="{{index}}" bindtap="ViewImage" data-url="{{imgList[index]}}">
				<image src='{{imgList[index]}}' mode='aspectFill'></image>
				<view class="cu-tag bg-red" catchtap="DelImg" data-index="{{index}}">
					<text class="cuIcon-close"></text>
				</view>
			</view>
			<view class="solids" bindtap="ChooseImage" wx:if="{{imgList.length<4}}">
				<text class="cuIcon-cameraadd"></text>
			</view>
		</view>
	</view>


      <!--uploadImage input-->


      <!--button input-->

     <view class="padding flex flex-direction" >
        <button bindtap="submitNewSlang" formType="submit" lang="zh_CN" class="cu-btn bg-gradual-blue margin-tb-sm lg">Enter</button>
     </view>
        
    <!--button input-->
    </view>
  </view>

</view>


